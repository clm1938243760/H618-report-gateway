# H618 报告网关 v0.22.4

## 升级体验

- 升级代理状态查询遇到本机端口短暂重启时自动进行有限重试。
- 升级页面不再把正常的代理重载窗口直接显示为英文连接错误。
- 网页服务在完整安装或开机启动时等待升级代理启动顺序。
- 检查、下载、安装和回滚等写操作不自动重试，避免重复执行任务。

## 升级说明

- 目标平台：`linux-arm64`
- 应用编码：`linux`
- 支持从 `v0.22.0` 至 `v0.22.3` 原子升级。
- 不覆盖 `/etc/gadget-msc-printer` 和 `/var/lib/gadget-msc-printer`。
